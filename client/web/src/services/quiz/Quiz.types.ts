// Moved to @freshr/shared; re-exported here for back-compat (removed in 1.4).
export type {
  QuizService,
  NotebookTopic,
  QuizGenerateOptions,
  QuizCreatePayload,
  QuizAnswerPayload,
} from "@freshr/shared";
