{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  /* Not used for compilation (files is empty) — the shadcn CLI reads the root
     tsconfig to discover the import alias when generating components. */
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
